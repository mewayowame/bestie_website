# 📘 BESTIEウェブサイト セットアップガイド

## 🎯 このガイドについて

このガイドは、BESTIEウェブサイト（バージョン4.2）を初めてセットアップする方のための詳細な手順書です。

---

## ✅ 必要な環境

開始前に以下がインストールされていることを確認してください：

1. **Node.js** (バージョン16以上推奨)
   - ダウンロード: https://nodejs.org/
   - インストール確認: ターミナルで `node --version` を実行

2. **npm** (Node.jsと一緒にインストールされます)
   - 確認: ターミナルで `npm --version` を実行

3. **テキストエディタ** (Visual Studio Code推奨)
   - ダウンロード: https://code.microsoft.com/

---

## 🚀 セットアップ手順

### ステップ1: プロジェクトフォルダを開く

1. ダウンロードしたZIPファイルを解凍
2. `bestie-website-v4.2` フォルダを好きな場所に配置
3. ターミナル（コマンドプロンプト）を開く
4. `cd` コマンドでプロジェクトフォルダに移動

```bash
cd path/to/bestie-website-v4.2
```

### ステップ2: 依存関係のインストール

プロジェクトに必要なパッケージをインストールします：

```bash
npm install
```

**所要時間**: 2-5分（インターネット速度による）

### ステップ3: 開発サーバーの起動

```bash
npm run dev
```

成功すると、ブラウザが自動的に開き `http://localhost:3000` でウェブサイトが表示されます。

---

## 🎨 開発中の編集方法

### ファイル編集の流れ

1. テキストエディタ（VSCode）でプロジェクトフォルダを開く
2. `src/App.jsx` を編集
3. 保存すると自動的にブラウザに反映される（ホットリロード）

### 主要ファイルの役割

```
src/
├── App.jsx          # メインコンポーネント（ここを主に編集）
├── main.jsx         # エントリーポイント（通常は編集不要）
└── index.css        # グローバルスタイル
```

---

## 📦 本番ビルド

### ビルドコマンド

```bash
npm run build
```

### ビルド結果

- `dist` フォルダが生成されます
- このフォルダの内容をWebサーバーにアップロードすれば本番公開完了

### ビルドのプレビュー

ビルド結果をローカルで確認：

```bash
npm run preview
```

---

## 🔧 カスタマイズ方法

### 1. 色の変更

`tailwind.config.js` の `colors` セクションを編集：

```javascript
colors: {
  'bestie-green': '#00af33',    // メインカラー
  'bestie-beige': '#eed1c9',    // アクセントカラー
  'bestie-dark': '#42433c',     // ポップアップ背景
  'bestie-red': '#fc4242',      // ボタン色
},
```

### 2. テキストの変更

`src/App.jsx` を開いて、変更したいテキストを検索・編集

例：ヒーローセクションのテキスト
```jsx
<h1>人の力が動き出すとき、組織は変わる</h1>
```

### 3. メールアドレスの変更

`src/App.jsx` の `handleSendEmail` 関数内：

```javascript
const mailtoLink = `mailto:info@bestie.co.jp?subject=...`
                           ↑
                    ここを変更
```

---

## 🐛 トラブルシューティング

### 問題: `npm install` がエラーになる

**原因**: Node.jsのバージョンが古い

**解決策**: 
1. https://nodejs.org/ から最新版をダウンロード
2. インストール後、ターミナルを再起動
3. `npm install` を再実行

---

### 問題: ブラウザが開かない

**原因**: ポート3000が使用中

**解決策**: 
1. `vite.config.js` を開く
2. `port: 3000` を `port: 3001` に変更
3. サーバーを再起動

---

### 問題: 画面が真っ白

**原因**: JavaScriptエラー

**解決策**:
1. ブラウザの開発者ツールを開く（F12キー）
2. Consoleタブでエラーを確認
3. エラーメッセージをコピーして検索

---

## 📞 サポート

### よくある質問

**Q: Reactの知識がなくても編集できますか？**
A: はい。テキストや色の変更は `App.jsx` を直接編集するだけでOKです。

**Q: デプロイはどうすれば？**
A: `npm run build` で生成された `dist` フォルダをレンタルサーバーにアップロードしてください。

**Q: スマホでの表示は？**
A: Tailwind CSSのレスポンシブクラスで対応済みです。自動的に最適化されます。

---

## 🎓 次のステップ

1. **React公式チュートリアル**: https://react.dev/learn
2. **Tailwind CSS公式ドキュメント**: https://tailwindcss.com/docs
3. **Vite公式ドキュメント**: https://vitejs.dev/guide/

---

## 📝 変更履歴

- **v4.2**: CONTACTポップアップ実装
- **v4.1**: PROFILEポップアップ、SVGロゴ統合
- **v4.0**: 初版リリース

---

🎉 **セットアップ完了！** 開発をお楽しみください！
